type Name = string;
let firstname: Name = '大樹';
let lastname: Name = '久恒';

type Password = number | string;
let password: Password = 'hisatune0423';

type User = { id: number; name: string };
const user: User = { id: 1, name: 'tune' };
